// error in (minus)(sqrt) , (minus)(sin),  JUMP (ASSIGN expression)

VAR floatc; 
VAR intd;
VAR inte;


FOR intd=1 STEP 1 TO 32 
    DO 
BEGIN
    floatc = -sqrt 4;
    inte = -sin 25;
    JUMP intf = 255;
END
